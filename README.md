# JavaScript RAG Chatbot

A Retrieval-Augmented Generation (RAG) chatbot that answers questions based on the content of a JavaScript HTML document.

The application uses **Gemini** for embeddings and response generation, **Pinecone** as the vector database, **LangChain** for the RAG pipeline, and **Streamlit** for the chatbot interface.

